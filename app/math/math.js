
const math = {
    sum: (a, b) => a + b,
    subtract: (a, b) => a - b,
    multiply: (a, b) => a * b,
    divide: (a, b) => a / b,
    sqrt: (x) => Math.sqrt(x),
    max: (a, b) => Math.max(a, b),
  };
  
  module.exports = math;